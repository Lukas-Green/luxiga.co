# GitHub Pages / Jekyll config for luxiga.co
# Keep this file minimal — the site is plain HTML, Jekyll is used only for its
# NOTE: this exclude list does NOT protect anything. The deploy workflow uses
# upload-pages-artifact, which bypasses the Jekyll build. Non-public paths are
# pruned in .github/workflows/ci.yml and gitignored. Keep both, not this.

exclude:
  - infrastructure
