// LUXIGA card runtime config.
// After deploying infrastructure/card-worker, set this to the Worker URL, e.g.
//   window.LX_API = "https://luxiga-card.your-subdomain.workers.dev";
// While empty, the work-request form falls back to mailto and events are not sent.
window.LX_API = "";
