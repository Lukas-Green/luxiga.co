.DS_Store
node_modules/
test-results/
playwright-report/

# --- Sensitive-document guard (added 2026-08-04) ---
# THIS REPO IS PUBLIC AND SERVES luxiga.co.
# Anything committed here is served at https://luxiga.co/<path> immediately.
# Never let entity, tax, or identity documents in.
*.pdf
!card/print/luxiga-business-card.pdf
# Allowed 2026-09-05. These two are deliberately public: they are the download
# targets of the primary buttons on card/resume/. Without these exceptions the
# blanket *.pdf rule kept them out of the deploy and both buttons 404'd, while
# still looking correct on disk. Their SOURCE lives in the private forge at
# business/resume-build/, never here. Add a PDF here only if it is meant to be
# world-readable at https://luxiga.co/<path>.
!card/resume/Lukas-Green-Resume.pdf
!card/resume/Lukas-Green-Cover-Letter.pdf
ARTICLES*OF*ORGANIZATION*
CP_575*
CP-575*
*EIN*
*W-9*
*W9*
*.p12
*.pem
.env
.env.*

# Non-public. This repo is PUBLIC and serves luxiga.co.
infrastructure/
SESSION.md

# Never publish: this repo is PUBLIC and Pages serves whatever is tracked.
# docs/ shipped the internal pricing playbook (naming Justin) at 200 until 2026-08-05.
docs/
scripts/
Idea rambling.moved-to-voicememo
